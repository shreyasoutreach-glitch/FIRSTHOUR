# Leave unset to use the relative "/api" path, which both the Vite dev
# server proxy (vite.config.ts) and the docker-compose nginx config forward
# to the backend container. Set this only if the frontend and backend are
# deployed on genuinely different origins.
VITE_API_BASE_URL=
